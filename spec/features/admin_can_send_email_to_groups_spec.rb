require 'spec_helper'

feature 'admin can send email' do
  it "xxx" do
    visit '/xx'
    expect(page).to have_content "All The Events"
  end

  it "xxx" do
    visit '/xxx'
    expect(page).to have_content "All The Events"
  end

end
